# Telegram bot for business-implementation-planner
# Get a NEW token from @BotFather — never commit the real token.
TELEGRAM_BOT_TOKEN=

# Key frames extracted per video (3–12, default 8). Requires ffmpeg.
VIDEO_MAX_FRAMES=8
